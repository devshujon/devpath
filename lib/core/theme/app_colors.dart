import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const primary  = Color(0xFF6C5CE7);
  static const accent   = Color(0xFF00D4AA);
  static const warning  = Color(0xFFFFB020);
  static const danger   = Color(0xFFEF4444);

  // Streak fire
  static const streakFlame = Color(0xFFFF6B35);

  // Light
  static const lightBg      = Color(0xFFFAFBFD);
  static const lightSurface = Colors.white;
  static const lightText    = Color(0xFF0F1419);
  static const lightSubtle  = Color(0xFF5D6670);

  // Dark
  static const darkBg       = Color(0xFF0A0C10);
  static const darkSurface  = Color(0xFF141820);
  static const darkText     = Color(0xFFECEEF2);
  static const darkSubtle   = Color(0xFF8A929D);
}
